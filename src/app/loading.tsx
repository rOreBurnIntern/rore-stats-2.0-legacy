export default function Loading() {
  return (
    <div className="app-shell flex min-h-screen flex-col overflow-x-auto font-sans">
      <main className="mx-auto flex min-h-screen w-full max-w-7xl min-w-0 items-center justify-center px-4 py-8">
        <p className="dashboard-muted text-lg font-medium">Loading...</p>
      </main>
    </div>
  );
}
