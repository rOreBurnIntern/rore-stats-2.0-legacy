export default function Loading() {
  return (
    <div className="flex min-h-screen flex-col overflow-x-auto bg-zinc-50 font-sans dark:bg-black">
      <main className="mx-auto flex min-h-screen w-full max-w-7xl min-w-0 items-center justify-center px-4 py-8">
        <p className="text-lg font-medium text-zinc-600 dark:text-zinc-300">Loading...</p>
      </main>
    </div>
  );
}
